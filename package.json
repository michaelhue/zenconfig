{
  "name": "zenconfig",
  "version": "1.0.0-beta.1",
  "description": "Simple schema-based configuration for twelve-factor apps",
  "homepage": "https://github.com/michaelhue/zenconfig",
  "bugs": "https://github.com/michaelhue/zenconfig/issues",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf .nyc_output coverage *.log",
    "lint": "eslint .",
    "precommit": "npm run prepublish",
    "prepublish": "npm run lint && npm run test-cov && npm run clean",
    "test": "tap lib/*_test.js",
    "test-cov": "npm test -- --check-coverage --lines=100",
    "test-lcov": "npm test -- --coverage-report=lcov"
  },
  "keywords": [
    "config",
    "configuration",
    "env",
    "environment"
  ],
  "author": {
    "name": "Michael Hüneburg",
    "email": "hello@michaelhue.com",
    "url": "https://michaehue.co"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/michaelhue/zenconfig"
  },
  "engines": {
    "node": ">=6.0"
  },
  "devDependencies": {
    "eslint": "^3.1.1",
    "pre-commit": "^1.1.3",
    "tap": "^6.2.0"
  }
}
